spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://db.ngfuwuiewhjubridtqnt.supabase.co:5432/postgres?sslmode=require}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:dh1792dhdh@}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ${JWT_SECRET:appclimb-secret-key-must-be-at-least-256-bits-long-for-hs256}
  access-token-expiration: 3600000
  refresh-token-expiration: 2592000000

server:
  port: ${PORT:8080}
