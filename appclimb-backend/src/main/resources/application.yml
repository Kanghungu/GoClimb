spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres?sslmode=require}
    username: ${SPRING_DATASOURCE_USERNAME:postgres.ngfuwuiewhjubridtqnt}
    password: ${SPRING_DATASOURCE_PASSWORD:dh1792dhdh@}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 2

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          lob:
            non_contextual_creation: true

jwt:
  secret: ${JWT_SECRET:appclimb-secret-key-must-be-at-least-256-bits-long-for-hs256}
  access-token-expiration: 3600000
  refresh-token-expiration: 2592000000

server:
  port: ${PORT:8080}
