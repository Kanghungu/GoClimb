spring:
  datasource:
    url: jdbc:postgresql://db.ngfuwuiewhjubridtqnt.supabase.co:5432/postgres
    username: postgres
    password: dh1792dhdh@
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate        # 테이블은 이미 Supabase에 생성했으므로 validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: appclimb-secret-key-must-be-at-least-256-bits-long-for-hs256
  access-token-expiration: 3600000       # 1시간 (ms)
  refresh-token-expiration: 2592000000   # 30일 (ms)

server:
  port: 8080
